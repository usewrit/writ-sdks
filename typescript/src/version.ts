/** SDK version — keep in sync with package.json. */
export const VERSION = "0.1.0";

/** `User-Agent` sent on every request (DESIGN.md §2: `writ-sdk-<lang>/<version>`). */
export const USER_AGENT = `writ-sdk-typescript/${VERSION}`;
